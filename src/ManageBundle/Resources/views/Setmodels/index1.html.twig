{% extends 'ManageBundle:layout:content-layout.html.twig' %}
{% block content %}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <!-- /.box-header -->
            <div class="dataTables_filter" id="searchDiv">
                <input placeholder="请输入模型名" name="name" class="form-control" type="search" likeOption="true"/>
                <div class="btn-group">
                    <button type="button" class="btn btn-primary" data-btn-type="search">查询</button>
                    <button type="button" class="btn btn-default" data-btn-type="reset">重置</button>
                </div>
                <div class="btn-group">
                    <button type="button" class="btn btn-default" data-btn-type="add">新增</button>
                    <button type="button" class="btn btn-default" data-btn-type="edit">编辑</button>
                    <button type="button" class="btn btn-default" data-btn-type="delete">删除</button>
                </div>
            </div>
            <div class="box-body">
                <table id="models_table" class="table table-border table-hover">
                </table>
            </div>
            <!-- /.box-body -->
        </div>
    </div>
    <!-- /.col -->
</div>
<!-- /.row -->
{% endblock %}
{% block javascripts %}
<script src="/bundles/manage/js/models.js" ></script>
<script>
	var winId="modelsWin";
	var modelsJs = ""; 
	var listurl = "{{ bundle }}/{{ controller }}/list";
    var editurl = "{{ bundle }}/{{ controller }}/edit";
    var saveurl = "{{ bundle }}/{{ controller }}/save";
    var delurl = "{{ bundle }}/{{ controller }}/delete";
    $(function() {
    	modelsJs = $("#searchDiv").models();
    });
</script>
{% endblock %}